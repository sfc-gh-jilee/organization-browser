// AUTO-GENERATED grid template for Text
// ⚠️  ADJUST BEFORE USING:
//   1. Fix imports (add subcomponents, icons as needed)
//   2. Adjust JSX for compound components (e.g., Tabs.Root > Tabs.List > Tabs.Trigger)
//   3. Use correct content pattern (children vs label prop)
//   4. Verify each variant renders correctly before extraction

import { Text } from '@snowflake/stellar-components'

export default function Reference() {
  return (
    <div data-component="text" style={{
      padding: '40px',
      display: 'flex',
      flexDirection: 'column',
      gap: '24px',
      background: '#ffffff',
      minHeight: '100vh',
      alignItems: 'flex-start',
    }}>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          caution / large
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="caution" size="large">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          caution / pageHeader
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="caution" size="pageHeader">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          caution / regular
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="caution" size="regular">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          caution / small
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="caution" size="small">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          critical / large
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="critical" size="large">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          critical / pageHeader
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="critical" size="pageHeader">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          critical / regular
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="critical" size="regular">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          critical / small
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="critical" size="small">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          primary / large
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="primary" size="large">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          primary / pageHeader
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="primary" size="pageHeader">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          primary / regular
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="primary" size="regular">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          primary / small
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="primary" size="small">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          secondary / large
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="secondary" size="large">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          secondary / pageHeader
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="secondary" size="pageHeader">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          secondary / regular
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="secondary" size="regular">Label</Text>
      </div>
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          secondary / small
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text variant="secondary" size="small">Label</Text>
      </div>

      {/* === STATES === */}
      <div style={{ display: 'flex', gap: '16px', alignItems: 'center' }}>
        <span style={{ minWidth: '180px', fontSize: '11px', color: '#888', fontFamily: 'monospace' }}>
          caution + disabled
        </span>
        {/* TODO: adjust for subcomponents, children vs label props */}
        <Text disabled variant="caution">Label</Text>
      </div>
    </div>
  )
}
